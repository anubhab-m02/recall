// Phase 2 stub. Extension entrypoint (activate/deactivate) — wires
// agentSupervisor, capture/*, and ui/* together. Imports the `vscode` API
// once real activation logic lands; kept dependency-free for the Phase 0
// scaffold build.

export function activatePlaceholder(): string {
  return "recall-vscode-extension";
}
