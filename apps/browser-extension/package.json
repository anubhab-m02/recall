{
  "name": "@recall/browser-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copyStaticAssets.mjs",
    "test": "vitest run",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@recall/shared-types": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.0",
    "fake-indexeddb": "^6.2.5",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
