// Phase 5 stub. MV3 background service worker: page-visit/search-query
// capture on allowlisted domains (spec FR-8), with a durable IndexedDB
// queue across worker restarts (spec NFR-REL-2).

export {};
