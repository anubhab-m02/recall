// Phase 5 stub. Popup UI: capture status, pause/resume, per-domain toggles
// (spec §13 Phase 5). React/JSX wiring lands with the Phase 5 build setup.

export function popupPlaceholder(): string {
  return "recall-popup";
}
